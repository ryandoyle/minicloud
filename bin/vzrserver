#!/usr/bin/env ruby

require File.expand_path(
  File.join(
    File.dirname(File.expand_path(
      File.symlink?(__FILE__) ? File.readlink(__FILE__) : __FILE__
    )),
    '../lib/vzruby'
  )
)

vzr = VZRuby::Server.new(8080)
vzr.run
